
# SSH Telegram Notify 🛡️

Простой скрипт для VPS, который присылает уведомления в Telegram каждый раз, когда кто-то подключается по SSH к серверу.  
Идеально для контроля доступа, безопасности и мгновенного оповещения о логинах.

---

## ⚡ Возможности

- Уведомление в Telegram при успешном входе по SSH.
- Работает для любого пользователя, включая `root`.
- Автоматическая настройка через одну команду.
- Минимальные зависимости: только `curl` (устанавливается автоматически, если отсутствует).
- Тестовое уведомление сразу после установки.
- Не блокирует входы, работает через PAM.

## ⚙️ Что делает скрипт

Скрипт полностью автоматизирует настройку уведомлений о входах по SSH в Telegram:

1. **Запрос Telegram Bot Token** 🗝️  
   - Скрипт попросит вставить токен, который выдаёт [BotFather](https://t.me/BotFather) в Telegram.  
   - Этот токен нужен для отправки сообщений через бота.

2. **Получение chat_id автоматически** 💬  
   - Скрипт попросит написать сообщение боту.  
   - После этого автоматически определится уникальный `chat_id` твоего чата.  
   - Не нужно искать его вручную.

3. **Создание скрипта уведомлений** 📄  
   - Файл создаётся в `/usr/local/bin/ssh_notify.sh`.  
   - При каждом входе по SSH он будет отправлять сообщение с:
     - Имя пользователя  
     - IP-адрес  
     - Дата и время  
     - Имя сервера

4. **Подключение к PAM** 🔗  
   - Добавляет вызов скрипта в `/etc/pam.d/sshd`, чтобы он запускался при каждой успешной авторизации SSH.

5. **Перезапуск SSH** 🔄  
   - Чтобы изменения вступили в силу, скрипт перезапускает службу SSH.

6. **Тестовое уведомление** ✅  
   - После установки скрипт отправит первое тестовое сообщение в Telegram, чтобы убедиться, что всё работает.

---

## 🚀 Установка

Всё делается **одной командой** на VPS (Ubuntu/Debian):📋
```bash
sudo bash -c "$(curl -s https://raw.githubusercontent.com/00firefly00/SSH-Telegram-Notify/main/TGNotify.sh)"
