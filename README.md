# SSH-Telegram-Notify

## Установка скрипта

Нажми на кнопку копирования и вставь команду в терминал на VPS:

```bash
sudo bash -c "$(curl -s https://raw.githubusercontent.com/00firefly00/SSH-Telegram-Notify/main/TGNotify.sh)"
