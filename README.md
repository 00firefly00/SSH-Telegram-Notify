
# SSH Telegram Notify 🛡️

Простой скрипт для VPS, который присылает уведомления в Telegram каждый раз, когда кто-то подключается по SSH к серверу.  
Идеально для контроля доступа, безопасности и мгновенного оповещения о логинах.

---

## ⚡ Возможности

- Уведомление в Telegram при успешном входе по SSH.
- Работает для любого пользователя, включая `root`.
- Автоматическая настройка через одну команду.
- Минимальные зависимости: только `curl` (устанавливается автоматически, если отсутствует).
- Тестовое уведомление сразу после установки.
- Не блокирует входы, работает через PAM.


   Что делает скрипт:

Запросит Telegram Bot Token (создаётся через BotFather).

Попросит написать сообщение боту, чтобы автоматически получить chat_id.

Создаст скрипт уведомлений /usr/local/bin/ssh_notify.sh.

Подключит его к PAM (/etc/pam.d/sshd).

Перезапустит SSH.

При первом входе отправит тестовое уведомление в Telegram.

---

## 🚀 Установка

Всё делается **одной командой** на VPS (Ubuntu/Debian):📋
```bash
sudo bash -c "$(curl -s https://raw.githubusercontent.com/00firefly00/SSH-Telegram-Notify/main/TGNotify.sh)"
